<div class="card">
    <img src="{{ asset('images/personagens/' . $image) }}" class="card-img-top" alt="{{ $name }}">
    <div class="card-body">
        <h5 class="card-title">{{ $name }}</h5>
    </div>
</div>
